export type Show = {
    title: string
    supportingbands?: string
    description?: string
    imageUrl?: string
    imageFile?: File
    venue: string
    city: string
    date: string
    time: string
    genre: string
}